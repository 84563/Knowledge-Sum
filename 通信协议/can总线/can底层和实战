先记录can总线的底层
一--简介：
 CAN：控制器区域网络（Controller Area Network）的缩写。CAN总线是一种适用于工业设备的高性能总线网络。STM32F103有1个CAN控制器，可以进行CAN总线网络的通信试验。

二---特点: 
 1多主控制：在总线空闲时，所有单元都可以发送消息（多主控制），无从主机之分。无地址，只要报文ID .
 2纠错能力强：具有CRC检测，检测出错误的单元会立即同时通知其他所有单元（错误通知功能），正在发送消息的单元一旦检测出错误，会强制结束当前的发送。强制结束发送的单元会不断反复地重新发送此消息直到成功发送为止（错误恢复功能）。
 3通信速度较快：通信距离远。最高1Mbps（距离小于40M），最远可达10KM（速率低于 5Kbps）。
 4非破坏性仲裁：当两个节点同时发送数据时，谁的 ID 小（优先级高），谁就能抢占总线。优先级低的节点会自动转入接收状态。
解释为什么ID中0的优先级高于1
  -总线电平分为显性电平和隐性电平，二者必居其一，显性电平对应逻辑0，CAN_H和CAN_L之差为2 V左右。而隐性电 平对应逻辑1，CAN_H和CAN_L之差为0V。
  -在总线上显性电平具有优先权，只要有一个单元输出显性电平，总线上即为显性电平。
  -而隐形电平则具有包容的意味，只有所有的单元都输出 隐性电平，总线上才为隐性电平（显性电平比隐性电平更强）
三--主要结构
主要介绍 数据帧（RTR=0）和遥控帧（RTR=1）
1--数据帧（发送数据给总线），标准帧（11位 ID）
SOF + 11位ID +  RTR + IDE + RB0 + 4位DLC + (0~8)数据段(遥控帧没有) + 16位CRC + 2位ACK + 7字节ERO
  起始位(SOF)一般为 0
  仲裁帧（ID+RTR）:RTR=0为数据帧
  控制段（IDE+RB0+DLC）：IDE控制是标准帧0还是扩展帧1。RB0保留位。DLC数据长度。
  数据段（）；0~8个字节即0-64bit
  CRC段；检查帧的传输错误的段。CRC共16位，其中前15位是CRC校验码，最后一位总是隐形电平，是CRC段的界定符
  ACK 段：包含 ACK 槽（发送方发1，接收方填0）和 ACK 界定符。
  结束帧（ERO）:连续 7 个隐性位 (1)，宣告本帧结束。
2--遥控帧
  无数据帧。RTR = 1.
3--扩展帧
SOF + 11位ID +  SSR + IDE+ 18位扩展ID +RTR+RB1+ RB0 + 4位DLC + (0~8)数据段(遥控帧没有) + 16位CRC + 2位ACK + 7字节ERO
  扩展帧的ID为29位。。
  扩展格式数据帧的仲裁段总共32位，包括11位标准ID、SRR位、IDE位（1）、18位扩展ID、RTR位。
  （标准格式的仲裁段是12位）扩展帧格式帧的IDE位总是隐形电平，表示这是扩展格式的帧。
  控制段。控制段由RB1位、RB0位和4位DLC组成。RB1位和RB0位是保留位，总是显性电平（0）。
  4位的DLC编码表示数据的长度，从0到8字节。

四--4种模式
1---静默模式；
  正常接收有效数据，不能发送数据到总线。
2---回环模式；
  可以正常向总线发送数据，但是不能接收总线的数据，只能接收自己的数据。自检模式什么都不需要。
3---静默回环模式；
  自发自收（检测自己数据，不会影响到CAN数据总线）
4---正常模式；
  向总线发送真实数据，同时也从总线接收真实数据。。
  必须有真实的 CAN 收发器，且总线上必须有其他节点回复 ACK，否则发 3 次直接报错卡死。

stm32f103c8t6（PB1 36M）
  每个CAN模块有3个发送邮箱，可自动重发。
  具有16位自主运行的定时器，可以定时触发通信，可以在最后两个数据字节发送时间戳。
  每个CAN模块有两个FIFO单元，每个FIFO有3个接收邮箱，每个FIFO有独立的中断地址。
  两个CAN模块共用28个筛选器组，筛选器用于配置可接收ID列表或掩码。数据帧和遥控帧根据ID被筛选，

--（只有波特率相等才能相互通信）
波特率 = 时钟频率 / (1 + BS1 + BS2) /Prescaler

BS1 是采样点之前的时间，用来吸收物理延迟。
BS2 是采样点之后的时间，用来补偿时钟误差。
BS1 + BS2 + 1 (同步段) = 1 个完整数据位的总长。

注意：使用cubemx配置的CAN工程不会自己启动CAN，需要自己调用HAL_CAN_Start()函数
重点：cubemx配置的CAN工程没有过滤配置，需要自己去配置，否则接收不到报文信息


  
